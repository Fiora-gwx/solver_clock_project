optimizer:
  candidate_count: 5
  data_samples: 32
  batch_size: 4
  sigma_data: 0.5
  seed: 0
  initial_steps: 10
  subdivision_rounds: 1
  max_iterations_initial: 2
  max_iterations_subdivision: 1
  early_stop:
    metric: fid
    proxy_num_samples: 32
    batch_size: 8
    patience: 1
    min_iterations: 1
    min_delta: 0.0
    seed_offset: 1000
